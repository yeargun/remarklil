import { jsString } from "../../parse/micromark/host.lil";
import { fail, opt } from "../js.lil";

export string checkStrong(JsValue state) {
  JsValue markerV = JS.get(opt(state), "strong");
  string marker = "*";
  if (markerV.truthy()) {
    marker = jsString(markerV);
  }
  if (marker != "*" && marker != "_") {
    fail("Cannot serialize strong with `" + marker + "` for `options.strong`, expected `*`, or `_`");
  }
  return marker;
}
