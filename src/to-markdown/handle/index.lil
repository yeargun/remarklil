import { blockquote } from "./blockquote.lil";
import { hardBreak } from "./break.lil";
import { code } from "./code.lil";
import { definition } from "./definition.lil";
import { emphasis } from "./emphasis.lil";
import { heading } from "./heading.lil";
import { html } from "./html.lil";
import { image } from "./image.lil";
import { imageReference } from "./image-reference.lil";
import { inlineCode } from "./inline-code.lil";
import { link } from "./link.lil";
import { linkReference } from "./link-reference.lil";
import { list } from "./list.lil";
import { listItem } from "./list-item.lil";
import { paragraph } from "./paragraph.lil";
import { root } from "./root.lil";
import { strong } from "./strong.lil";
import { text } from "./text.lil";
import { thematicBreak } from "./thematic-break.lil";

export JsValue defaultHandlers = JS.object();
JS.set(defaultHandlers, "blockquote", blockquote);
JS.set(defaultHandlers, "break", hardBreak);
JS.set(defaultHandlers, "code", code);
JS.set(defaultHandlers, "definition", definition);
JS.set(defaultHandlers, "emphasis", emphasis);
JS.set(defaultHandlers, "hardBreak", hardBreak);
JS.set(defaultHandlers, "heading", heading);
JS.set(defaultHandlers, "html", html);
JS.set(defaultHandlers, "image", image);
JS.set(defaultHandlers, "imageReference", imageReference);
JS.set(defaultHandlers, "inlineCode", inlineCode);
JS.set(defaultHandlers, "link", link);
JS.set(defaultHandlers, "linkReference", linkReference);
JS.set(defaultHandlers, "list", list);
JS.set(defaultHandlers, "listItem", listItem);
JS.set(defaultHandlers, "paragraph", paragraph);
JS.set(defaultHandlers, "root", root);
JS.set(defaultHandlers, "strong", strong);
JS.set(defaultHandlers, "text", text);
JS.set(defaultHandlers, "thematicBreak", thematicBreak);
